# gstack local state
.gstack/

# Secrets / environment
.env
.env.*
!.env.example
*.local

# macOS
.DS_Store

# Editors
.vscode/
.idea/
.obsidian/
*.swp

# Logs
*.log
logs/

# Node
node_modules/
dist/
build/
.cache/
npm-debug.log*
yarn-error.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Run-doc cache (api/run_cache.py FileRunStore) — content-addressed run-docs persisted at
# runtime to skip re-billing identical free-text questions. Runtime-only, never committed.
api/.run_cache/

# Coverage (pytest-cov / vitest v8 — measured in CI, never committed)
.coverage
coverage/
htmlcov/
coverage.xml

# Azure
.azure/
local.settings.json

# Video clips — source clips live in Azure Blob, not git (avoid repo bloat).
# Force-add a small curated clip with `git add -f` if you really want it tracked.
*.mov
*.mp4
*.mkv
# Local frame-extraction scratch
/frames/
*.frames/

.claude/worktrees

# Gstack
.context

# Claude
*.lock