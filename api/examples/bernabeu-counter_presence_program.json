{
  "_comment": "Pinned presence program for 'Is there a player on the field?' on bernabeu-counter. Hits the detect -> answer existence op-path: op_answer dispatches on the `detections` binding kind and grounds Yes by non-emptiness. Authored during eval-bank authoring per the D1 presence caveat; executed over the committed bernabeu cache to derive its ground-truth answer ('Yes'). Window 10000-10001ms @ fps1 samples the single legible frame (10000) whose cache carries 5 person detections. Validates against ../schema/dsl.schema.json. end_ms 10001 <= clip duration_ms 30086.",
  "program": [
    { "id": "frames", "op": "sample_frames", "args": { "start_ms": 10000, "end_ms": 10001, "fps": 1 } },
    { "id": "people", "op": "detect", "args": { "frames": "frames", "classes": ["person"] } },
    { "id": "result", "op": "answer", "args": { "from": "people", "question": "Is there a player on the field?" } }
  ]
}
