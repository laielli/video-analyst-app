# DSL schema validation
jsonschema>=4.21

# Azure AI Vision Image Analysis 4.0 — detect (OBJECTS) + read_text (READ/OCR)
azure-ai-vision-imageanalysis>=1.0.0
azure-core>=1.30

# Azure OpenAI gpt-4o vision — accurate read_text backend for stylized jersey numbers
openai>=1.40

# Web: FastAPI + SSE wrapper around the interpreter
fastapi>=0.110
uvicorn[standard]>=0.29

# Tests (api/tests/). The precompute pipeline mocks Azure + ffmpeg, so no SDK/ffmpeg is
# needed for those — only jsonschema (above) + pytest. The widened suite adds:
#   - pytest-asyncio: the SSE cancel test is `async` + `athrow` (asyncio_mode=auto in pytest.ini);
#     without it those tests collection-error in a clean CI install.
#   - httpx: FastAPI's TestClient needs it (only transitive via fastapi today — pin it so a
#     clean `pip install -r requirements.txt` is sufficient).
#   - pytest-cov: non-gating coverage reporting in CI (never --cov-fail-under).
pytest>=8.0
pytest-mock>=3.12
pytest-asyncio>=0.23
httpx>=0.27
pytest-cov>=5.0
