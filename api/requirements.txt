# DSL schema validation
jsonschema>=4.21

# Azure AI Vision Image Analysis 4.0 — detect (OBJECTS) + read_text (READ/OCR)
azure-ai-vision-imageanalysis>=1.0.0
azure-core>=1.30

# Azure OpenAI gpt-4o vision — accurate read_text backend for stylized jersey numbers
openai>=1.40

# Web: FastAPI + SSE wrapper around the interpreter
fastapi>=0.110
uvicorn[standard]>=0.29

# Tests (first pytest suite, api/tests/). The precompute pipeline mocks Azure + ffmpeg, so
# no SDK/ffmpeg is needed to run the suite — only jsonschema (above) + pytest.
pytest>=8.0
pytest-mock>=3.12
# SSE-stream cancel test is async (@pytest.mark.asyncio + body_iterator.athrow); without this
# pytest-asyncio dep + asyncio_mode=auto in pytest.ini, the async test collection-errors in CI.
pytest-asyncio>=0.23
# FastAPI TestClient (SSE-stream tests) needs httpx; today it's only transitive via fastapi.
httpx>=0.27
# Non-gating coverage report (CI runs --cov; never --cov-fail-under).
pytest-cov>=5.0
