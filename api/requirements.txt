# DSL schema validation
jsonschema>=4.21

# Azure AI Vision Image Analysis 4.0 — detect (OBJECTS) + read_text (READ/OCR)
azure-ai-vision-imageanalysis>=1.0.0
azure-core>=1.30

# Azure OpenAI gpt-4o vision — accurate read_text backend for stylized jersey numbers
openai>=1.40

# Web: FastAPI + SSE wrapper around the interpreter
fastapi>=0.110
uvicorn[standard]>=0.29

# Tests (first pytest suite, api/tests/). The precompute pipeline mocks Azure + ffmpeg, so
# no SDK/ffmpeg is needed to run the suite — only jsonschema (above) + pytest.
pytest>=8.0
pytest-mock>=3.12
# SSE/async coverage: the cancel test is @pytest.mark.asyncio (athrow), so pytest-asyncio is
# required (absent -> collection error in CI). httpx backs FastAPI's TestClient (transitive via
# fastapi today, pinned explicitly so the SSE-stream tests don't depend on that transitive).
# pytest-cov is non-gating coverage only (never --cov-fail-under). starlette is intentionally
# NOT pinned — fastapi already constrains it.
pytest-asyncio>=0.23
httpx>=0.27
pytest-cov>=5.0
