# DSL schema validation
jsonschema>=4.21

# Azure AI Vision Image Analysis 4.0 — detect (OBJECTS) + read_text (READ/OCR)
azure-ai-vision-imageanalysis>=1.0.0
azure-core>=1.30

# Azure OpenAI gpt-4o vision — accurate read_text backend for stylized jersey numbers
openai>=1.40

# Web: FastAPI + SSE wrapper around the interpreter
fastapi>=0.110
uvicorn[standard]>=0.29

# Tests (first pytest suite, api/tests/). The precompute pipeline mocks Azure + ffmpeg, so
# no SDK/ffmpeg is needed to run the suite — only jsonschema (above) + pytest.
pytest>=8.0
pytest-mock>=3.12
# Async SSE cancel test (@pytest.mark.asyncio + body_iterator.athrow) needs pytest-asyncio;
# absent it collection-errors the async test in CI. asyncio_mode=auto is set in pytest.ini.
pytest-asyncio>=0.23
# TestClient drives the SSE StreamingResponse; httpx is transitive via fastapi today, pinned
# explicitly so a clean CI install always has it.
httpx>=0.27
# Non-gating coverage measurement (never --cov-fail-under). starlette is left to fastapi.
pytest-cov>=4.1
