[pytest]
testpaths = tests
# `.` makes `interpreter`, `vision`, `canned` importable; `scripts` makes `precompute`,
# `ocr_probe`, `run_program`, `validate_program`, `validate_run_doc` importable — matching
# the scripts-importing-scripts path hack the CLIs use at runtime.
pythonpath = . scripts
addopts = -q
